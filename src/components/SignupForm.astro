---
---

<form
  action="https://buttondown.email/api/emails/embed-subscribe/garrettwelson"
  method="post"
  target="popupwindow"
  onsubmit="window.open('https://buttondown.email/garrettwelson', 'popupwindow')"
  class="embeddable-buttondown-form"
>
  <label class="formElement" for="bd-email">
    <p class="formIntro">Want to stay in touch?</p>
    <p>Enter your email to join my mailing list</p></label
  >
  <input
    type="email"
    name="email"
    id="bd-email"
    placeholder="merlin@wizardZone.info"
    class="emailInput formElement"
  />
  <button class="submitButton formElement" type="submit">Subscribe</button>
</form>

<style>
  form {
    display: flex;
    flex-direction: column;
    font-size: 14pt;
    border-top: 1px solid hsla(251, 10%, 42%, 0.4);
    border-bottom: 1px solid hsla(251, 10%, 42%, 0.4);
    padding: 0.75em 0.5em;
    margin: 1em 0 1em 0;
    background: var(--magnolia);
    justify-items: space-between;
  }

  .submitButton {
    background: var(--dark-liver);
    color: white;
    display: block;
    width: 100%;
    border-radius: 1rem;
    padding: 0.25rem;
    font-family: "Inter", sans-serif;
  }

  .emailInput {
    outline: 3px dotted --var(dark-liver);
    width: 100%;
    border-radius: 1rem;
    padding: 0.25rem;
  }

  .formIntro {
    font-weight: bold;
    font-size: 16pt;
  }

  .formElement {
    margin: 0.5em 0;
  }
</style>
